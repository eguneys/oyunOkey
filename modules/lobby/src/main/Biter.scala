package oyun.lobby

import actorApi.{ JoinHook, LobbyUser }
import oyun.game.{ GameRepo, Game, Player }
import okey.{ Game => OkeyGame, Table, Player => OkeyPlayer, Side }

private[lobby] object Biter {

  // def apply(hook: Hook, uid: String, user: Option[LobbyUser]): Option[JoinChallenge] =
  //   canJoin(hook, user) option join(hook, uid, user)

  // private def join(hook: Hook, uid: String, lobbyUserOption: Option[LobbyUser]): JoinChallenge =
  // {
  //   JoinChallenge(uid)
  // }

  // def canJoin(hook: Hook, user: Option[LobbyUser]): Boolean =
  //   true
}
